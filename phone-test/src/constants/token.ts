export const enum TokenStorageKey {
  ACCESS = 'access_token',
  REFRESH = 'refresh_token',
  EXPIRATION = 'token_expiration'
}

export const TOKEN_EXPIRATION_MINUTES = 9;

export const TOKEN_EXPIRED_URL_PARAM = 'token_expired';
